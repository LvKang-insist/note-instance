Synchronized 的本质：

​	Synchronized 实际上做的有两件事，**一个是资源的互斥访问；一个是线程之间的数据同步**；

- 互斥访问：调用 被Synchronized 声明的方法后 ，别的线程必须等待用完后在进行调用，并别的线程无法访问该对象中其他使用 synchronized 声明的方法。因为方法上的 synchronized 的 参数默认是当前对象
- 线程之间数据同步：

